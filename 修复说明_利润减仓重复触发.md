# 🔧 修复说明 - 利润减仓同一阶梯重复触发

## 问题

你的日志显示：
```
22:15:04  profit_reduce  target=18%  profit=+18.6%  ✅ 第一次触发
22:21:40  profit_reduce  target=18%  profit=+21.4%  ❌ 同一阶梯再次触发
```

**问题**: 利润从 18.6% 涨到 21.4%，仍在 18% 阶梯（18-24% 范围），不应该再次触发。

## 已实施的修复

### 1. 增强日志输出

现在会显示详细的状态信息：

```go
[Grid] Profit-reduce short: profitPct=21.40% targetStep=18% alreadyReduced=18% step=6%
[Grid] Profit-reduce short: skipping — already reduced at 18% (current target 18%)
```

**关键字段**:
- `profitPct`: 当前利润百分比
- `targetStep`: 根据当前利润计算的应触发阶梯
- `alreadyReduced`: 已经触发过的阶梯
- `step`: 配置的阶梯步长

### 2. 记录状态变化

下单成功后会记录：

```go
[Grid] Profit-reduce short: state updated from 0% to 18%
```

这样可以追踪状态是否正确更新。

### 3. 增强测试

添加了针对性的单元测试，覆盖：
- ✅ 同一阶梯不重复触发
- ✅ 下一阶梯正确触发
- ✅ 已存在订单时跳过

## 如何验证修复

### 部署新版本后，观察日志

#### 正常情况（修复生效）

**首次触发（18%）**:
```
[Grid] Profit-reduce short: profitPct=18.60% targetStep=18% alreadyReduced=0% step=6%
[Grid] Profit-reduce: short reducing 1.8 (target=18%)
[Grid] Profit-reduce short: state updated from 0% to 18%
```

**利润上涨（21%）- 应跳过**:
```
[Grid] Profit-reduce short: profitPct=21.40% targetStep=18% alreadyReduced=18% step=6%
[Grid] Profit-reduce short: skipping — already reduced at 18% (current target 18%)
```

**下一阶梯（25%）- 应触发**:
```
[Grid] Profit-reduce short: profitPct=25.20% targetStep=24% alreadyReduced=18% step=6%
[Grid] Profit-reduce: short reducing 2.4 (target=24%)
[Grid] Profit-reduce short: state updated from 18% to 24%
```

#### 异常情况（状态丢失）

如果看到：
```
[Grid] Profit-reduce short: profitPct=21.40% targetStep=18% alreadyReduced=0% step=6%
                                                                            ^^^ 问题！
```

说明 `alreadyReduced=0%`（应该是 18%），状态丢失了。

**可能原因**:
1. 交易员在两次触发之间重启了
2. 状态恢复逻辑有 bug

**请提供**:
- 完整的启动日志（查找 "Restored short profit-reduce progress"）
- 两次触发之间是否有重启
- 完整的 checkProfitReduce 日志

## 下一步

### 立即行动

1. **更新代码**:
```bash
cd /Users/drk/nofx
git pull origin main  # 或手动应用修改
go build .
```

2. **部署测试**:
- 先在测试环境部署
- 配置小仓位观察
- 收集完整日志

3. **观察日志**:
重点关注这些日志：
- 启动时: `[Grid] Restored short profit-reduce progress from log`
- 检查时: `[Grid] Profit-reduce short: profitPct=... alreadyReduced=...`
- 更新时: `[Grid] Profit-reduce short: state updated from ... to ...`

### 发生重复触发时

请提供以下信息：

1. **启动日志**（完整）:
```
[Grid] AI cycle monitor started...
[Grid] Restored short profit-reduce progress from log: XX%
```

2. **首次触发日志**:
```
[Grid] Profit-reduce short: profitPct=... targetStep=... alreadyReduced=...
[Grid] Profit-reduce: short reducing...
[Grid] Profit-reduce short: state updated from ... to ...
```

3. **重复触发日志**:
```
[Grid] Profit-reduce short: profitPct=... targetStep=... alreadyReduced=...  ← 关键！
```

4. **时间线**:
- 首次触发时间
- 重复触发时间
- **中间是否有重启**

5. **配置**:
```yaml
profit_reduce_step_pct: ?  # 你的配置值
ai_trigger_tf: ?          # AI 触发周期
scan_interval: ?          # 扫描间隔
```

## 临时缓解措施

在根本原因确认前，可以：

### 方案 1: 增加步长

```yaml
# 当前（假设）
profit_reduce_step_pct: 6  # 阶梯: 6%, 12%, 18%, 24%...

# 调整为
profit_reduce_step_pct: 10  # 阶梯: 10%, 20%, 30%, 40%...
```

更大的步长意味着更少的触发频率，降低重复触发的影响。

### 方案 2: 监控 + 手动取消

看到重复触发时：
1. 立即取消多余的减仓订单
2. 记录完整日志
3. 反馈给开发者

### 方案 3: API 重置状态

如果状态确实丢失，可以手动重置：

```bash
curl -X POST http://localhost:8080/api/v1/traders/{trader_id}/profit-reduce/reset \
  -H "Content-Type: application/json" \
  -d '{"side": "short"}'
```

## 代码修改位置

如果你需要自己应用修改：

### 文件 1: `trader/auto_trader_grid.go`

**位置 1**: 约 1260-1280 行（添加调试日志）

```go
// 在 checkProfitReduce() 函数中
alreadyReduced := at.gridState.LongProfitReducedPct
if info.side == "short" {
    alreadyReduced = at.gridState.ShortProfitReducedPct
}
targetReducePct := math.Floor(profitPct/step) * step

// ↓↓↓ 添加这段 ↓↓↓
logger.Infof("[Grid] Profit-reduce %s: profitPct=%.2f%% targetStep=%.0f%% alreadyReduced=%.0f%% step=%.0f%%",
    info.side, profitPct, targetReducePct, alreadyReduced, step)
// ↑↑↑ 添加结束 ↑↑↑

if targetReducePct <= alreadyReduced {
    // ↓↓↓ 添加这段 ↓↓↓
    logger.Infof("[Grid] Profit-reduce %s: skipping — already reduced at %.0f%% (current target %.0f%%)",
        info.side, alreadyReduced, targetReducePct)
    // ↑↑↑ 添加结束 ↑↑↑
    continue
}
```

**位置 2**: 约 1400-1425 行（记录状态更新）

```go
at.gridState.mu.Lock()
if a.closeAll {
    if info.side == "long" {
        at.gridState.LongProfitReducedPct = 0
    } else {
        at.gridState.ShortProfitReducedPct = 0
    }
    // ↓↓↓ 添加这段 ↓↓↓
    logger.Infof("[Grid] Profit-reduce %s: state updated to 0%% (closeAll)", info.side)
    // ↑↑↑ 添加结束 ↑↑↑
} else {
    newPct := a.targetReducePct
    // ↓↓↓ 添加这段 ↓↓↓
    oldPct := at.gridState.LongProfitReducedPct
    if info.side == "short" {
        oldPct = at.gridState.ShortProfitReducedPct
    }
    // ↑↑↑ 添加结束 ↑↑↑
    if info.side == "long" {
        at.gridState.LongProfitReducedPct = newPct
    } else {
        at.gridState.ShortProfitReducedPct = newPct
    }
    // ↓↓↓ 添加这段 ↓↓↓
    logger.Infof("[Grid] Profit-reduce %s: state updated from %.0f%% to %.0f%%", 
        info.side, oldPct, newPct)
    // ↑↑↑ 添加结束 ↑↑↑
}
at.gridState.mu.Unlock()
```

## 总结

- ✅ 代码逻辑本身是正确的（`if targetReducePct <= alreadyReduced { continue }`）
- 🔍 问题可能是状态丢失（重启、恢复失败等）
- 📊 已添加详细日志帮助诊断
- ⏳ 需要收集实际日志确认根本原因
- 🛠️ 根据日志分析结果实施针对性修复

---

**关键**: 下次发生时，请提供完整日志，特别是 `alreadyReduced=?%` 的值！

**最后更新**: 2026-06-24
