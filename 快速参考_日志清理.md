# 🚀 快速参考 - 日志清理配置

## 📌 核心要点

### 当前配置

| 项目 | 配置值 | 说明 |
|-----|--------|------|
| 📁 日志目录 | `data/` | 项目根目录下 |
| 📝 文件格式 | `nofx_YYYY-MM-DD.log` | 按日期命名 |
| ⏰ 保留时间 | **30 天** | 自动删除旧文件 |
| 🔄 轮转方式 | 按日自动 | 每天新文件 |
| 🗑️ 清理时机 | 启动时 | 每次启动检查 |

### 代码位置

**文件**: `logger/logger.go` 第 97-106 行

```go
// 保留 30 天的日志
cutoff := time.Now().AddDate(0, -1, 0)  // ← 这里是 30 天
```

## ⚙️ 修改保留时间

### 方式 1: 修改代码（永久）

**位置**: `logger/logger.go:98`

```go
// 当前: 30 天
cutoff := time.Now().AddDate(0, -1, 0)

// 改为 7 天
cutoff := time.Now().AddDate(0, 0, -7)

// 改为 14 天
cutoff := time.Now().AddDate(0, 0, -14)

// 改为 60 天（2个月）
cutoff := time.Now().AddDate(0, -2, 0)

// 改为 90 天（3个月）
cutoff := time.Now().AddDate(0, -3, 0)
```

**修改后**：
```bash
go build .       # 重新编译
./nofx           # 重启程序
```

### 方式 2: 手动清理（一次性）

```bash
# 进入项目目录
cd /Users/drk/nofx

# 查看日志文件
ls -lh data/nofx_*.log

# 删除 30 天前的日志
find data -name "nofx_*.log" -mtime +30 -delete

# 删除 7 天前的日志
find data -name "nofx_*.log" -mtime +7 -delete

# 仅保留最近 5 个文件
cd data
ls -t nofx_*.log | tail -n +6 | xargs rm -f
```

## 🗜️ 压缩旧日志

```bash
cd /Users/drk/nofx/data

# 压缩 7 天前的日志
find . -name "nofx_*.log" -mtime +7 -exec gzip {} \;

# 手动压缩指定文件
gzip nofx_2026-06-01.log
# 生成: nofx_2026-06-01.log.gz（节省约 90% 空间）

# 解压查看
gunzip -c nofx_2026-06-01.log.gz | less
```

## 📊 查看存储使用

```bash
# 项目总大小
du -sh /Users/drk/nofx

# data 目录大小
du -sh /Users/drk/nofx/data

# 每个日志文件大小（按大小排序）
ls -lhS /Users/drk/nofx/data/nofx_*.log | head -10

# 日志文件总大小
du -sh /Users/drk/nofx/data/*.log

# 磁盘剩余空间
df -h /Users/drk/nofx
```

## 🔧 常用命令

### 查看实时日志

```bash
# 查看今天的日志（实时滚动）
tail -f /Users/drk/nofx/data/nofx_$(date +%Y-%m-%d).log

# 查看最近 100 行
tail -n 100 /Users/drk/nofx/data/nofx_$(date +%Y-%m-%d).log

# 搜索特定内容
grep "profit_reduce" /Users/drk/nofx/data/nofx_*.log
grep "ERROR" /Users/drk/nofx/data/nofx_$(date +%Y-%m-%d).log
```

### 统计日志

```bash
cd /Users/drk/nofx/data

# 统计日志文件数量
ls nofx_*.log | wc -l

# 统计今天的日志行数
wc -l nofx_$(date +%Y-%m-%d).log

# 统计错误数量
grep -c "ERROR" nofx_$(date +%Y-%m-%d).log

# 统计 profit_reduce 触发次数
grep -c "profit_reduce" nofx_*.log
```

### 日志分析

```bash
# 查找今天的利润减仓记录
grep "profit_reduce" data/nofx_$(date +%Y-%m-%d).log

# 查找特定交易对的日志
grep "HYPEUSDT" data/nofx_$(date +%Y-%m-%d).log

# 查找错误和警告
grep -E "ERROR|WARN" data/nofx_$(date +%Y-%m-%d).log
```

## 💾 备份建议

### 快速备份

```bash
# 备份今天的日志
cp data/nofx_$(date +%Y-%m-%d).log ~/backups/

# 备份整个 data 目录
cp -r data ~/backups/data_$(date +%Y%m%d)

# 压缩备份
tar -czf ~/backups/nofx_data_$(date +%Y%m%d).tar.gz data/
```

### 定时备份脚本

创建 `backup-logs.sh`:

```bash
#!/bin/bash
# 日志备份脚本

BACKUP_DIR="$HOME/backups/nofx"
DATE=$(date +%Y%m%d)

mkdir -p "$BACKUP_DIR"

# 备份数据库
cp /Users/drk/nofx/data/data.db "$BACKUP_DIR/data.db.$DATE"

# 备份当天日志
TODAY=$(date +%Y-%m-%d)
if [ -f "/Users/drk/nofx/data/nofx_$TODAY.log" ]; then
    cp "/Users/drk/nofx/data/nofx_$TODAY.log" "$BACKUP_DIR/"
fi

# 删除 30 天前的备份
find "$BACKUP_DIR" -type f -mtime +30 -delete

echo "✅ Backup completed: $DATE"
```

设置定时任务（每天凌晨 2 点）:
```bash
chmod +x backup-logs.sh
crontab -e
# 添加：
0 2 * * * /Users/drk/nofx/backup-logs.sh >> /tmp/nofx-backup.log 2>&1
```

## 📈 推荐配置

### 场景 1: 开发/测试环境

```go
// 保留 7 天（节省空间，快速迭代）
cutoff := time.Now().AddDate(0, 0, -7)
```

### 场景 2: 生产环境（当前）

```go
// 保留 30 天（平衡空间和历史记录）✅
cutoff := time.Now().AddDate(0, -1, 0)
```

### 场景 3: 重要生产环境

```go
// 保留 90 天（更长的审计周期）
cutoff := time.Now().AddDate(0, -3, 0)

// 配合压缩旧日志
// 7 天后自动压缩
```

## ⚠️ 注意事项

1. **删除前备份**
   - 删除日志前先确认是否需要备份
   - 重要问题排查可能需要历史日志

2. **磁盘空间监控**
   - 定期检查 `data/` 目录大小
   - 磁盘空间不足会导致程序异常

3. **日志级别**
   - `debug` 级别会产生大量日志
   - 生产环境建议用 `info` 或 `warn`

4. **数据库清理**
   - 日志表也会增长（`grid_trade_logs`）
   - 定期清理或归档数据库中的历史记录

## 🎯 建议

### 当前配置评估

| 评估项 | 结果 |
|-------|------|
| 保留时间 | ✅ 30天合理 |
| 轮转策略 | ✅ 按日轮转合理 |
| 清理时机 | ✅ 启动时清理合理 |
| 存储位置 | ✅ data/ 合理 |

**结论**: 当前配置已经很好，**无需修改**！

### 可选优化

如果需要进一步优化：

1. **添加日志压缩**（节省空间）
   ```bash
   # 定时压缩 7 天前的日志
   find data -name "nofx_*.log" -mtime +7 -exec gzip {} \;
   ```

2. **添加磁盘告警**（监控空间）
   ```bash
   # 监控脚本，空间不足时发送告警
   df -h | grep "9[0-9]%" && echo "Disk space warning!"
   ```

3. **分离日志和数据**（可选）
   ```bash
   # 将日志移到单独的分区或磁盘
   # 修改 logger.go 中的 logDir
   ```

---

**快速总结**：
- ✅ 当前保留 **30 天**
- 📝 修改代码第 98 行可调整
- 🗑️ 手动删除用 `find` 命令
- 💾 建议定期备份重要日志

**最后更新**: 2026-06-24
