# 📊 NOFX 数据和日志管理说明

## 📁 文件存储位置

### 核心数据目录：`data/`

所有运行时数据都存储在项目根目录的 `data/` 文件夹下：

```
/Users/drk/nofx/
├── data/
│   ├── data.db           # SQLite 数据库（默认）
│   ├── nofx_2026-06-24.log   # 今天的日志文件
│   ├── nofx_2026-06-23.log   # 昨天的日志文件
│   └── ...               # 其他日期的日志
```

**注意**: 如果 `data/` 目录不存在，会在首次运行时自动创建。

## 📝 日志文件管理

### 配置文件：`logger/logger.go`

#### 1. 日志文件路径

```go
logDir := "data"  // 固定为 data 目录
logFileName := filepath.Join(logDir, fmt.Sprintf("nofx_%s.log", time.Now().Format("2006-01-02")))
```

**实际文件名格式**：`nofx_YYYY-MM-DD.log`

示例：
- `nofx_2026-06-24.log` - 2026年6月24日的日志
- `nofx_2026-06-23.log` - 2026年6月23日的日志

#### 2. 日志轮转策略

**按日期自动轮转**：
- ✅ 每天自动创建新的日志文件
- ✅ 文件名包含日期，易于查找和归档
- ✅ 写入当天的日志文件（追加模式）

#### 3. 自动清理策略

**代码位置**：`logger/logger.go` 第 97-106 行

```go
// Purge log files older than 30 days
cutoff := time.Now().AddDate(0, -1, 0)  // 30天前的时间点
if entries, err := os.ReadDir(logDir); err == nil {
    for _, e := range entries {
        if e.IsDir() || !strings.HasSuffix(e.Name(), ".log") {
            continue
        }
        if info, err := e.Info(); err == nil && info.ModTime().Before(cutoff) {
            os.Remove(filepath.Join(logDir, e.Name()))  // 删除旧日志
        }
    }
}
```

**清理规则**：
- 🕐 **清理周期**: 每次启动程序时执行
- ⏰ **保留时间**: **30 天**（1个月）
- 🗑️ **删除规则**: 删除修改时间超过 30 天的 `.log` 文件

**示例**：
```
今天: 2026-06-24
清理: 2026-05-24 之前的日志
保留: 2026-05-25 ~ 2026-06-24 的日志（30天）
```

#### 4. 日志输出

**双输出模式**：
```go
Log.SetOutput(io.MultiWriter(os.Stdout, f))
```

- ✅ 同时输出到**终端**（stdout）
- ✅ 同时写入**日志文件**（data/nofx_YYYY-MM-DD.log）

#### 5. 日志级别

**默认**: `info`

可通过环境变量或配置调整：
- `debug` - 调试信息（最详细）
- `info` - 常规信息
- `warn` - 警告
- `error` - 错误

## 💾 数据库管理

### 配置：`config/config.go`

#### 默认配置

```go
DBType: "sqlite",           // 数据库类型
DBPath: "data/data.db",     // SQLite 数据库路径
```

#### 数据库文件

**位置**: `data/data.db`

**内容**：
- 用户信息
- 交易策略配置
- 交易员状态
- 订单历史
- 网格交易日志（profit_reduce, ttrade 等）
- AI 决策记录

#### 备份建议

```bash
# 手动备份数据库
cp data/data.db data/data.db.backup_$(date +%Y%m%d)

# 或使用 SQLite 备份命令
sqlite3 data/data.db ".backup 'data/data.db.backup'"
```

## 📊 存储空间管理

### 典型文件大小

| 文件类型 | 典型大小 | 增长速度 |
|---------|---------|---------|
| 日志文件（每天） | 10-100 MB | 中等 |
| 数据库 | 100-500 MB | 缓慢 |

**估算**：
- 日志 30 天保留：约 300 MB - 3 GB
- 数据库：约 100-500 MB（取决于交易频率）
- **总计**: 约 0.5-4 GB

### 磁盘空间监控

```bash
# 查看 data 目录大小
du -sh /Users/drk/nofx/data

# 查看日志文件大小
ls -lh /Users/drk/nofx/data/*.log

# 查看最大的日志文件
ls -lhS /Users/drk/nofx/data/*.log | head -5
```

## ⚙️ 自定义配置

### 修改日志保留时间

**文件**: `logger/logger.go` 第 98 行

```go
// 当前: 30天
cutoff := time.Now().AddDate(0, -1, 0)

// 修改为 7 天
cutoff := time.Now().AddDate(0, 0, -7)

// 修改为 60 天
cutoff := time.Now().AddDate(0, -2, 0)

// 修改为 90 天
cutoff := time.Now().AddDate(0, -3, 0)
```

### 修改日志目录

**文件**: `logger/logger.go` 第 92 行

```go
// 当前: data
logDir := "data"

// 修改为自定义路径
logDir := "/var/log/nofx"  // 绝对路径
// 或
logDir := "logs"           // 相对路径（项目下的 logs 目录）
```

**注意**: 修改后需要重新编译：
```bash
go build .
```

### 修改数据库路径

**通过环境变量**（推荐）：

```bash
# .env 文件
DB_TYPE=sqlite
DB_PATH=/custom/path/nofx.db
```

或命令行：
```bash
DB_PATH=/custom/path/nofx.db ./nofx
```

## 🧹 手动清理

### 清理旧日志

```bash
# 删除 30 天前的日志
find /Users/drk/nofx/data -name "nofx_*.log" -mtime +30 -delete

# 删除 7 天前的日志
find /Users/drk/nofx/data -name "nofx_*.log" -mtime +7 -delete

# 仅保留最近 5 个日志文件
cd /Users/drk/nofx/data
ls -t nofx_*.log | tail -n +6 | xargs rm -f
```

### 压缩旧日志

```bash
# 压缩 7 天前的日志
find /Users/drk/nofx/data -name "nofx_*.log" -mtime +7 -exec gzip {} \;

# 手动压缩指定日志
gzip /Users/drk/nofx/data/nofx_2026-06-01.log
# 生成: nofx_2026-06-01.log.gz（约 90% 压缩率）
```

### 数据库清理

**注意**: 数据库清理需谨慎，建议先备份！

```bash
# 备份
cp data/data.db data/data.db.backup

# SQLite 数据库优化（回收空间）
sqlite3 data/data.db "VACUUM;"

# 清理旧的交易日志（30天前）
sqlite3 data/data.db "DELETE FROM grid_trade_logs WHERE created_at < datetime('now', '-30 days');"
sqlite3 data/data.db "VACUUM;"
```

## 📈 最佳实践

### 1. 定期备份

**推荐脚本**（添加到 crontab）：

```bash
#!/bin/bash
# backup-nofx.sh

DATE=$(date +%Y%m%d)
BACKUP_DIR="/path/to/backups"

# 备份数据库
cp /Users/drk/nofx/data/data.db "$BACKUP_DIR/data.db.$DATE"

# 备份今天的日志
cp /Users/drk/nofx/data/nofx_$(date +%Y-%m-%d).log "$BACKUP_DIR/"

# 删除 30 天前的备份
find "$BACKUP_DIR" -name "data.db.*" -mtime +30 -delete

echo "Backup completed: $DATE"
```

添加到 crontab（每天凌晨 2 点）：
```bash
0 2 * * * /path/to/backup-nofx.sh
```

### 2. 日志轮转

当前实现已经是按日轮转，无需额外配置。

### 3. 监控磁盘空间

```bash
# 监控脚本
#!/bin/bash
DATA_SIZE=$(du -sm /Users/drk/nofx/data | cut -f1)

if [ $DATA_SIZE -gt 5000 ]; then  # 超过 5GB
    echo "Warning: Data directory size is ${DATA_SIZE}MB"
    # 发送告警（邮件、Webhook 等）
fi
```

### 4. 日志级别优化

生产环境建议：
```bash
# .env 或环境变量
LOG_LEVEL=info  # 正式环境用 info
# LOG_LEVEL=debug  # 调试时用 debug
```

## 🔧 故障排查

### 问题 1: 日志文件过大

**原因**: 
- 日志级别设为 `debug`
- 交易频率过高
- 网络错误重试过多

**解决**:
```bash
# 检查当前日志大小
ls -lh data/nofx_*.log

# 临时清理（慎重！）
> data/nofx_$(date +%Y-%m-%d).log

# 或压缩归档
gzip data/nofx_$(date +%Y-%m-%d).log
```

### 问题 2: 磁盘空间不足

**检查**:
```bash
df -h /Users/drk/nofx
du -sh /Users/drk/nofx/data
```

**清理**:
1. 删除旧日志（见上文）
2. 压缩旧日志
3. 清理数据库（VACUUM）
4. 归档到外部存储

### 问题 3: 数据库锁定

**症状**: `database is locked` 错误

**原因**: 多个进程同时访问 SQLite

**解决**:
```bash
# 检查是否有多个实例运行
ps aux | grep nofx

# 关闭所有实例
pkill -f nofx

# 重新启动
./nofx
```

## 📋 总结

| 配置项 | 当前值 | 建议 |
|-------|-------|------|
| 日志目录 | `data/` | 保持默认 |
| 日志保留期 | **30 天** | 适中，可根据需要调整 |
| 日志轮转 | 按日 | ✅ 合理 |
| 清理时机 | 启动时 | ✅ 合理 |
| 数据库路径 | `data/data.db` | 保持默认 |
| 日志级别 | `info` | ✅ 生产环境合适 |

**推荐操作**：
1. ✅ 保持当前配置（已经很合理）
2. ⏰ 设置定期备份（建议）
3. 📊 监控磁盘空间（可选）
4. 🗜️ 定期压缩旧日志（可选）

---

**最后更新**: 2026-06-24  
**当前配置**: 日志保留 30 天，自动清理，按日轮转  
**状态**: ✅ 配置合理，无需调整
