# 🔄 浮盈减仓进度重置说明

## 问题回答

**问：当我重置了浮盈减仓进度，`alreadyReduced` 也会重置吗？**

**答：是的！✅** 重置浮盈减仓进度会将 `alreadyReduced` 重置为 `0`。

---

## 🔍 详细说明

### 重置功能代码

**文件**: `trader/auto_trader_grid.go` 第 1437-1453 行

```go
func (at *AutoTrader) ResetProfitTracker(side string) {
    if at.gridState == nil {
        return
    }
    gridConfig := at.config.StrategyConfig.GridConfig
    at.gridState.mu.Lock()
    if side == "long" {
        at.gridState.LongProfitReducedPct = 0  // ← 重置为 0
    } else {
        at.gridState.ShortProfitReducedPct = 0  // ← 重置为 0
    }
    at.gridState.mu.Unlock()
    logger.Infof("[Grid] Profit-reduce tracker manually reset: %s", side)
    at.logGridTrade("profit_reduce", "profit_reduce_reset", side,
        gridConfig.Symbol, "manual reset via dashboard", "", 0, 0, 0, 0, 0, 0, true, "")
}
```

### 执行流程

```
1. 用户在 Dashboard 点击"重置浮盈减仓进度"
   ↓
2. 前端调用 API: POST /api/v1/traders/{id}/grid-risk/reset-profit-tracker
   Body: {"side": "short"}  或  {"side": "long"}
   ↓
3. 后端执行 ResetProfitTracker("short")
   ↓
4. 更新内存状态：ShortProfitReducedPct = 0
   ↓
5. 写入日志："profit_reduce_reset" + "manual reset via dashboard"
   ↓
6. 完成！下次检查时 alreadyReduced = 0
```

## 📊 重置效果

### 重置前

```
持仓: SHORT entry=63.12 mark=60.80
利润: 18.47%
状态: alreadyReduced = 18%

检查结果:
profitPct=18.47% targetStep=18% alreadyReduced=18%
→ skipping（跳过，不触发）
```

### 重置后

```
持仓: SHORT entry=63.12 mark=60.80
利润: 18.47%
状态: alreadyReduced = 0%  ← 已重置

检查结果:
profitPct=18.47% targetStep=18% alreadyReduced=0%  ← 注意这里
→ 触发减仓（会再次下单）✅
```

## ⚠️ 重置影响

### 1. 立即生效

重置后，下一次 AI 周期检查时：
- `alreadyReduced` 变为 `0`
- 如果当前利润 > 0，会立即触发相应阶梯的减仓

### 2. 会重复减仓

**示例场景**：

```
原始状态:
- 利润 18%，已触发 18% 阶梯
- alreadyReduced = 18%
- 状态：跳过

用户手动重置 →

新状态:
- 利润仍然 18%
- alreadyReduced = 0%  ← 重置
- 状态：会再次触发 18% 阶梯 ⚠️

结果:
- 会再次下减仓单！
- 可能导致过度减仓
```

### 3. 日志记录

重置操作会写入日志：

```
[Grid] Profit-reduce tracker manually reset: short
📉 profit_reduce profit_reduce_reset SHORT manual reset via dashboard
```

**重要**: 写入 `profit_reduce_reset` 日志后，程序重启时：
- 不会从旧的 `profit_reduce` 日志恢复状态
- 会从 `profit_reduce_reset` 日志读取，确认状态已重置

## 🎯 使用场景

### 适合重置的情况

✅ **场景 1: 状态错误**
```
问题: alreadyReduced 状态不正确（如卡在旧值）
解决: 手动重置，让系统重新开始跟踪
```

✅ **场景 2: 持仓重新开始**
```
问题: 平仓后重新开仓，想从 0 开始计算
解决: 重置进度，重新触发阶梯
```

✅ **场景 3: 策略调整**
```
问题: 修改了 profit_reduce_step_pct，想重新触发
解决: 重置进度，按新配置执行
```

### 不适合重置的情况

❌ **场景 1: 持仓还在，仅想再次减仓**
```
问题: 利润在同一阶梯内，想再次减仓
风险: 重置后会立即触发，可能过度减仓
建议: 不要重置，等利润涨到下一阶梯自然触发
```

❌ **场景 2: 想立即平仓**
```
问题: 想快速平仓所有持仓
风险: 重置会触发减仓，但不是全平
建议: 直接使用"平仓"功能，而不是重置减仓进度
```

## 🔄 自动重置机制

系统在以下情况会**自动重置**（无需手动操作）：

### 1. 持仓平仓后

**代码位置**: `trader/auto_trader_grid.go` 第 1211-1219 行

```go
// 检测到没有持仓时自动重置
if _, hasLong := sides["long"]; !hasLong && at.gridState.LongProfitReducedPct > 0 {
    logger.Infof("[Grid] Profit-reduce: resetting long tracker (no active long position)")
    at.gridState.LongProfitReducedPct = 0
}
if _, hasShort := sides["short"]; !hasShort && at.gridState.ShortProfitReducedPct > 0 {
    logger.Infof("[Grid] Profit-reduce: resetting short tracker (no active short position)")
    at.gridState.ShortProfitReducedPct = 0
}
```

**触发条件**:
- SHORT 持仓完全平仓（没有 SHORT 持仓）
- LONG 持仓完全平仓（没有 LONG 持仓）

**日志**:
```
[Grid] Profit-reduce: resetting short tracker (no active short position)
```

### 2. 利润变为负数或零

**代码位置**: `trader/auto_trader_grid.go` 第 1297-1324 行

```go
if profitPct <= 0 {
    // 利润 <= 0，重置状态
    actions = append(actions, reduceAction{
        info: *info, 
        qty: 0, 
        closeAll: false, 
        targetReducePct: -1  // -1 表示重置
    })
    continue
}

// 执行重置
if a.targetReducePct == -1 {
    at.gridState.mu.Lock()
    if info.side == "long" {
        prev = at.gridState.LongProfitReducedPct
        at.gridState.LongProfitReducedPct = 0
    } else {
        prev = at.gridState.ShortProfitReducedPct
        at.gridState.ShortProfitReducedPct = 0
    }
    at.gridState.mu.Unlock()
    if prev > 0 {
        at.logGridTrade("profit_reduce", "profit_reduce_reset", info.side, symbol,
            "profit went to zero/negative — reset tracker", "", 0, 0, 0, 0, 0, 0, true, "")
    }
}
```

**触发条件**:
- 持仓浮盈变为 0 或负数（浮亏）

**日志**:
```
📉 profit_reduce profit_reduce_reset SHORT profit went to zero/negative — reset tracker
```

## 📋 API 使用

### 请求

```bash
POST /api/v1/traders/{trader_id}/grid-risk/reset-profit-tracker
Content-Type: application/json

{
  "side": "short"  # 或 "long"
}
```

### 响应

成功：
```json
{
  "message": "tracker reset"
}
```

错误：
```json
{
  "error": "side must be 'long' or 'short'"
}
```

### cURL 示例

```bash
# 重置 SHORT 的浮盈减仓进度
curl -X POST "http://localhost:8080/api/v1/traders/YOUR_TRADER_ID/grid-risk/reset-profit-tracker" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{"side": "short"}'

# 重置 LONG 的浮盈减仓进度
curl -X POST "http://localhost:8080/api/v1/traders/YOUR_TRADER_ID/grid-risk/reset-profit-tracker" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{"side": "long"}'
```

## 🔍 验证重置效果

### 1. 查看日志

```bash
# 查看重置日志
grep "profit_reduce_reset" data/nofx_$(date +%Y-%m-%d).log

# 预期输出
[Grid] Profit-reduce tracker manually reset: short
📉 profit_reduce profit_reduce_reset SHORT manual reset via dashboard
```

### 2. 观察下次检查

重置后，下一次 AI 周期会显示：

```
[Grid] Profit-reduce short: profitPct=18.47% targetStep=18% alreadyReduced=0% step=6%
                                                                            ^^^ 已重置为 0
[Grid] Profit-reduce: short reducing 1.8 (target=18%)  ← 会再次触发
```

### 3. 检查状态更新

```
[Grid] Profit-reduce short: state updated from 0% to 18%  ← 从 0 开始重新记录
```

## ⚡ 注意事项

### 1. 重置 ≠ 撤销订单

重置浮盈减仓进度**不会**：
- ❌ 撤销已存在的减仓订单
- ❌ 影响已下单但未成交的订单
- ❌ 改变持仓本身

重置只会：
- ✅ 将 `alreadyReduced` 状态重置为 0
- ✅ 下次检查时重新触发

### 2. 可能立即下单

重置后，如果：
- 当前持仓有利润（> 0）
- 利润达到某个阶梯（如 18%）

系统会**立即触发减仓**（几秒到几分钟内，取决于 AI 周期）

### 3. 不影响历史日志

重置操作：
- ✅ 写入新的 `profit_reduce_reset` 日志
- ✅ 不会删除或修改历史的 `profit_reduce` 日志
- ✅ 重启后正确识别重置状态

## 📖 总结

| 问题 | 答案 |
|------|------|
| 重置会清空 alreadyReduced 吗？ | ✅ 是，重置为 0 |
| 重置后会立即触发吗？ | ✅ 可能，取决于当前利润 |
| 重置会撤销已有订单吗？ | ❌ 不会 |
| 持仓平仓后会自动重置吗？ | ✅ 会 |
| 利润变负会自动重置吗？ | ✅ 会 |
| 重置后可以恢复吗？ | ❌ 不能，需要等待自然触发 |

**核心要点**：
- 重置 = 将 `alreadyReduced` 清零
- 重置后系统会重新开始跟踪浮盈减仓进度
- 如果当前有利润，会立即触发相应阶梯的减仓
- 谨慎使用，避免过度减仓

---

**最后更新**: 2026-06-24  
**代码位置**: `trader/auto_trader_grid.go:1437-1453`  
**API 路径**: `POST /api/v1/traders/{id}/grid-risk/reset-profit-tracker`
